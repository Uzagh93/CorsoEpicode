#include <stdio.h>

void menu ();
void moltiplica ();
void dividi ();
void ins_string();

int main () 
{
    char scelta = '0';
    do {
        menu ();
        scanf(" %c", &scelta);
        switch (scelta)
        {
            case 'A':
                moltiplica();
                break;
            case 'B':
                dividi();
                break;
            case 'C':
                ins_string();
                break;
            default:
                printf("Scelta non valida.\n");
        }
    } while(scelta = '0');   
    return 0;
}
void menu ()
{
    printf ("Benvenuto, sono un assistente digitale, posso aiutarti a sbrigare alcuni compiti\n");
    printf ("Come posso aiutarti?\n");
    printf ("A >> Moltiplicare due numeri\nB >> Dividere due numeri\nC >> Inserire una stringa\n");
    }
void moltiplica ()
{
    int a ,b= 0;
    printf ("Inserisci i due numeri da moltiplicare: ");
    scanf ("%d", &a);
    scanf ("%d", &b);
    int prodotto = a* b;
    printf ("Il prodotto tra %d e %d è: %d\n", a, b, prodotto);
}

void dividi ()
{
    float a,b = 0;
    printf ("Inserisci il numeratore: ");
    scanf("%f", &a);
    printf ("Inserisci il denominatore: ");
    scanf("%f", &b);

    if (b == 0) {
        printf("È impossibile dividere per zero\n");
        return 0;
    }
    float divisione = a / b;
    printf ("La divisione tra %0.2f e %0.2f è: %0.2f\n", a, b, divisione);
}
void ins_string () 
{
    char stringa[50];
    printf ("Inserisci la stringa: ");
    scanf("%s", &stringa);
    prntf("Hai inserito: %s", stringa);
    return 0;
}